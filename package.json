{
  "name": "vue-wade-admin",
  "version": "1.0.0",
  "author": {
    "name": "wade",
    "email": "853925809@qq.com",
    "url": "https://github.com/wadv-code/vue-wade-admin"
  },
  "private": true,
  "scripts": {
    "dev": "node-run dev",
    "serve": "turbo run dev",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "lint": "turbo run lint",
    "api": "pnpm -F @wade/api run dev",
    "web": "pnpm -F @wade/web run dev",
    "backend": "pnpm -F @wade/backend run dev",
    "docs": "pnpm -F @wade/docs run dev",
    "check": "biome check --write",
    "format": "biome format --write",
    "check-types": "turbo run check-types",
    "postinstall": "pnpm -r run stub --if-present",
    "preinstall": "npx only-allow pnpm",
    "update:deps": "npx taze -r -w",
    "catalog": "pnpx codemod pnpm/catalog",
    "changeset": "pnpm exec changeset",
    "version": "pnpm exec changeset version && pnpm install --no-frozen-lockfile"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@changesets/cli": "catalog:",
    "@rsbuild/core": "catalog:",
    "@types/node": "catalog:",
    "@vue/tsconfig": "catalog:",
    "@wade/node-run": "workspace:^",
    "@wade/postcss-config": "workspace:^",
    "@wade/tsconfig": "workspace:^",
    "tailwindcss": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "unbuild": "catalog:"
  },
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=18"
  }
}
